services:
  web:
    build: 
      context: .
      args:
        - VITE_API_BASE_URL=http://localhost:8080/api/v1/
    restart: unless-stopped
    container_name: nginx-blurgames-front
    ports:
      - '80:80'